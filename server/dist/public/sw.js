// Generated by LiveScript 1.5.0
(function(){
  var this$ = this;
  this.addEventListener('fetch', function(e){
    var req, url, headers, newReq;
    req = e.request;
    url = new URL(req.url);
    if (url.host === 'res.cloudinary.com' && !req.headers.get('width')) {
      headers = new Headers({
        width: 1000
      });
      req.headers.forEach(function(v, k){
        return headers.set(k, v);
      });
      newReq = new Request(req, {
        mode: 'cors',
        credentials: 'omit',
        headers: headers
      });
      return e.respondWith(fetch(newReq));
    }
  });
}).call(this);

//# sourceMappingURL=sw.js.map

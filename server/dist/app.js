// Generated by LiveScript 1.5.0
(function(){
  var keystone, fecha, path, fs;
  require('require-self-ref');
  require('marko/express');
  require('marko/node-require').install();
  keystone = require('keystone');
  fecha = require('fecha');
  path = require('path');
  fs = require('fs');
  fecha.masks.long = 'dddd D MMMM, YYYY';
  keystone.init({
    'name': 'colinaarts.com',
    'favicon': 'public/favicon.ico',
    'static': ['public'],
    'cors allow origin': true,
    'views': 'templates/views',
    'view engine': 'marko',
    'auto update': true,
    'mongo': fs.readFileSync(path.resolve(__dirname, '../../secure/.mlab'), 'utf-8').trim(),
    'session': true,
    'session store': 'mongo',
    'auth': true,
    'user model': 'User',
    'cookie secret': fs.readFileSync(path.resolve(__dirname, '../../secure/.cookiesecret'), 'utf-8'),
    'logger': '[:date[web]] :method :url :status (:response-time ms)',
    'compress': false,
    'ssl': true,
    'ssl key': path.resolve(__dirname, '../../secure/key.pem'),
    'ssl cert': path.resolve(__dirname, '../../secure/cert.pem'),
    'cloudinary config': fs.readFileSync(path.resolve(__dirname, '../../secure/.cloudinary'), 'utf-8'),
    'cloudinary folders': true,
    'cloudinary secure': true
  });
  require('./models');
  keystone.set('routes', require('./routes'));
  keystone.start();
}).call(this);

//# sourceMappingURL=app.js.map
